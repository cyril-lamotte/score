/**
 * @file App initialisation & domready event.
 *
 * Only the "app" variable is exposed to window's context.
 */
(function($) {

"use strict";

window.app = {
  dbName: 'score',
  dbVersion: 1
};

})();
